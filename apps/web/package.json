{
  "name": "@ono-toolkit/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --host 127.0.0.1",
    "build": "nuxt build",
    "typecheck": "nuxt prepare && vue-tsc --noEmit"
  }
}
